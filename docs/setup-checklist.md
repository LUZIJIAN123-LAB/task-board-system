# 🚀 环境配置快速指南

本小姐为你准备的超详细配置步骤！跟着做就对了～

## ✅ 已完成检查

- [x] Java环境（需要重启终端验证17）
- [x] Maven 3.9.9
- [x] Node.js 22.17.0
- [x] npm 10.9.2
- [x] MySQL 8.0.35
- [x] Redis（已下载）

---

## 📝 当前需要做的事

### 1. 验证Java 17 ⭐⭐⭐

**重要！请先做这一步：**

```bash
# 1. 关闭当前所有命令行窗口（包括IDE的终端）
# 2. 重新打开一个新的命令行窗口
# 3. 验证Java版本

java -version

# 应该显示类似：
# java version "17.0.x"
# Java(TM) SE Runtime Environment ...
```

**如果还是显示Java 11：**
- 检查环境变量JAVA_HOME是否正确指向JDK 17
- 检查Path变量中%JAVA_HOME%\bin是否在最前面
- 可能需要重启电脑

---

### 2. 启动Redis服务器 ⭐⭐⭐

**方法1：直接运行（推荐测试用）**

```bash
# 进入你下载的Redis目录，比如：
cd F:\Redis

# 启动Redis服务器（保持窗口打开）
redis-server.exe redis.windows.conf

# 你应该看到类似这样的输出：
# [port] Redis 5.x.x (00000000/0) 64 bit
# [port] Server started, Redis version 5.x.x
# [port] Ready to accept connections
```

**方法2：安装为Windows服务（推荐长期使用）**

```bash
# 以管理员身份运行命令行
cd F:\Redis

# 安装服务
redis-server.exe --service-install redis.windows.conf --service-name Redis

# 启动服务
redis-server.exe --service-start

# 以后可以用这些命令管理：
# redis-server.exe --service-stop    # 停止服务
# redis-server.exe --service-start   # 启动服务
```

**测试Redis是否正常：**

```bash
# 新开一个命令行窗口
cd F:\Redis
redis-cli.exe

# 在redis-cli中输入：
ping

# 应该返回：
PONG

# 退出redis-cli
exit
```

---

### 3. 初始化MySQL数据库 ⭐⭐⭐

**方法1：使用自动脚本（推荐）**

```bash
# 双击运行这个文件：
task-board-system/sql/init-database.bat

# 按提示输入MySQL用户名和密码
# 用户名：root
# 密码：你的MySQL密码
```

**方法2：手动执行（如果脚本不work）**

```bash
# 1. 打开命令行，进入sql目录
cd F:\code\project\task-board-system\sql

# 2. 登录MySQL
mysql -u root -p
# 输入密码

# 3. 执行SQL脚本
source init.sql

# 或者直接在命令行执行
mysql -u root -p < init.sql

# 4. 验证数据库创建成功
mysql -u root -p
use task_board;
show tables;
# 应该显示10个表
```

---

### 4. 配置后端项目 ⭐⭐

**修改配置文件：**

编辑文件：`task-board-system/backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_board?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root          # 你的MySQL用户名
    password: 你的MySQL密码   # ⚠️ 改成你的实际密码

  data:
    redis:
      host: localhost
      port: 6379
      password:             # 如果Redis设置了密码就填写，没有就留空
      database: 0
```

---

### 5. 安装前端依赖 ⭐⭐

```bash
# 进入前端目录
cd task-board-system/frontend

# 安装依赖（可能需要几分钟）
npm install

# 如果下载速度慢，可以使用国内镜像：
npm install --registry=https://registry.npmmirror.com
```

---

## 🎯 完整的启动流程

### 前置条件检查：
```bash
☑️ Redis服务器正在运行（redis-server.exe窗口打开着）
☑️ MySQL服务正在运行
☑️ task_board数据库已创建
☑️ Java 17环境变量已配置
```

### 启动步骤：

**步骤1：启动后端**
```bash
# 打开新的命令行窗口
cd F:\code\project\task-board-system\backend

# 验证Java版本（应该是17）
java -version

# 启动后端（第一次会下载Maven依赖，需要等待）
mvn spring-boot:run

# 启动成功的标志：
# 看到 "✨ 任务看板系统启动成功！"
# 或者 "Started TaskBoardApplication in xxx seconds"
```

**步骤2：启动前端**
```bash
# 打开新的命令行窗口
cd F:\code\project\task-board-system\frontend

# 启动前端开发服务器
npm run dev

# 启动成功后会自动打开浏览器
# 或者手动访问：http://localhost:3000
```

---

## 🌐 访问地址

启动成功后：
- 🎨 **前端页面**：http://localhost:3000
- 🔌 **后端API**：http://localhost:8080/api
- 📖 **API文档**：http://localhost:8080/api/doc.html

---

## 🧪 测试账号

```
用户名：admin
密码：123456

其他测试账号：
用户名：user1 / user2
密码：123456
```

---

## ❓ 常见问题

### Q1: Maven下载依赖很慢？
**A:** 配置阿里云镜像：
```xml
编辑文件：C:\Users\你的用户名\.m2\settings.xml
添加阿里云镜像配置（参考development-setup.md）
```

### Q2: 启动报错"端口被占用"？
**A:**
```bash
# 查看占用端口的进程
netstat -ano | findstr :8080

# 结束进程
taskkill /PID 进程号 /F

# 或者修改配置文件中的端口号
```

### Q3: Redis连接失败？
**A:** 检查：
1. Redis服务器是否在运行
2. 端口6379是否被占用
3. 防火墙是否阻止连接

### Q4: MySQL连接失败？
**A:** 检查：
1. MySQL服务是否启动
2. application.yml中的用户名密码是否正确
3. task_board数据库是否已创建

---

## 📞 需要帮助？

如果遇到任何问题，请告诉本小姐：
1. 具体的错误信息
2. 你在执行哪一步
3. 控制台输出的日志

本小姐会帮你解决的！(^_^)b

---

**完成所有步骤后，你就可以开始开发了！加油，笨蛋！** ✨
